influxdb:
  url: "http://localhost:8086"
  token: "iot-lab-super-secret-auth-token"
  org: "iot-lab"
  bucket: "fbp-metrics"
  batch:
    size: 1000
    flush_interval_ms: 10000
  retry:
    max_attempts: 5
    initial_backoff_ms: 200
  buffer:
    type: "memory"
    max_size: 100000